<Window x:Class="LeagueReplay.Replay.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LeagueReplay.Replay.UI"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="MainWindow" SizeToContent="WidthAndHeight">
  <Grid Name="ClientArea" Width="800" Height="500">
    <Grid.RowDefinitions>
      <RowDefinition Height="20"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <ListBox Name="ListItems" ItemsSource="{Binding SortedReplays}" Grid.Row="1" BorderThickness="0"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="-1" MouseUp="MatchClickUp">
      <ListBox.ItemContainerStyle>
        <Style TargetType="ListBoxItem">
          <Setter Property="Padding" Value="0"/>
          <Setter Property="Margin" Value="-1"/>
          <Setter Property="BorderThickness" Value="0"/>
        </Style>
      </ListBox.ItemContainerStyle>
    </ListBox>
    
    <TextBox Name="SearchBox" Padding="1 1 1 1" BorderThickness="0 0 0 1" VerticalAlignment="Center"/>
  </Grid>
</Window>