<Window x:Class="LeagueReplay.Replay.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LeagueReplay.Replay.UI"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="MainWindow" SizeToContent="WidthAndHeight">
  <Grid Name="ClientArea" Width="800" Height="500">
    <Grid.RowDefinitions>
      <RowDefinition Height="20"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <TextBox Name="SearchBox" Height="20" Padding="1,1,1,1" Margin="0,0,0,0" VerticalAlignment="Top"/>

    <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled" Grid.Row="1">
      <ItemsControl Name="ListItems" ItemsSource="{Binding SortedReplays}"/>
    </ScrollViewer>
  </Grid>
</Window>